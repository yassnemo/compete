name: compete
version: "0.1.0"
config-version: 2

profile: compete

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  compete:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
